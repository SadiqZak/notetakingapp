import { extendTheme } from "@chakra-ui/react";

const config = {
    initialColorMode: "light",
}

const colors = {
    yellow:"#F4B400"
}

// const styles = {

// }

// const components = {

// }

export const theme = extendTheme({
    colors,
    config
})